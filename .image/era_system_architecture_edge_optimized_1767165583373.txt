A clean, content-rich technical diagram of the 'Edge-RAG-Anything (ERA)' architecture. Style: Professional Academic Infographic (Original Style). Minimalist 2D/Flat style. White background. Color Palette: Slate Blue (Core), Sage Green (Domain), Amber (Logic).

Layout:
1. **Edge Environment Container**: Enclose the ENTIRE system (except User/Devices) in a clear, rounded rectangle labeled **'Edge Device (Max 8GB RAM)'**. This is the most important new element.

2. **Input**: 'User Query' -> **'Intent Analysis'**.

3. **Path A (Question)**:
   - 'Intent Analysis' -> **'Hybrid Retrieval'** (Dense/Sparse/PathRAG).
   - -> **'Dynamic Weighting'** -> **'Resource Check'**.
   - -> **'Qwen3-4B'**.

4. **Path B (Task Request)**:
   - 'Intent Analysis' -> **'LLM (Action Plan)'**.
   - -> **'User Feedback/Permission'**.
   - -> **'Device Controller'**.
   - -> **'Execution Result'**.
   - -> **'Qwen3-4B'**.

5. **Unified Output**:
   - All paths converge to **'Qwen3-4B'** -> **'Final Answer'**.

6. **Visuals**: Emphasize that all computation happens INSIDE the Edge Device container. External elements are only 'User', 'Smart Farm Devices', and 'Final Answer'.
