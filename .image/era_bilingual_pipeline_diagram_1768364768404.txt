A detailed, high-fidelity technical architecture diagram of 'ERA-SmartFarm-RAG' with BILINGUAL labels (Korean + English).
Style: Professional Engineering Diagram. Clean, High Contrast, White Background.
Layout: 3 Main Horizontal Sections (A, B, C) plus a Header and Footer.

**Header**: "Edge Device Environment (8GB RAM | Q4_K_M | Offline)"

**Section A: Knowledge Ingestion Pipeline (지식 수집 파이프라인)**
- Flow: [Data Sources (데이터 소스)] -> [Text Extraction (텍스트 추출)] -> [Embedding (임베딩)] -> [Index & Graph Build (인덱스 구축)] -> [Knowledge Store (지식 저장소)]
- **Knowledge Store Box**: Contains [Dense Index (밀집)], [Sparse Index (희소)], [Causal Graph (인과)], [Ontology (온톨로지)].

**Cache Layer (캐시 계층)**
- [Query Cache (질의 캐시)] [Embedding Cache (임베딩 캐시)] [Response Cache (응답 캐시)]

**Section B: Hybrid Retrieval Pipeline (하이브리드 검색 파이프라인)**
- Flow: [Query Analysis (질의 분석)] -> [3-Channel Search (3채널 검색)] -> [Context Shaping (컨텍스트 조정)]

**Section C: Answer Generation Pipeline (답변 생성 파이프라인)**
- Flow: [Prompt Builder (프롬프트 빌더)] -> [LLM Engine (LLM 엔진)] -> [Response (응답)]
- **Fallback**: Arrow from LLM Engine (failure) -> [Fallback Chain (폴백 체인)].

**Footer**: "No Cloud Required | Full Pipeline On-Device | Data Never Leaves Device"

Visuals:
- Use distinct bounding boxes for Sections A, B, C.
- Use clear arrows (->) to show the flow.
- Color Code: Section A (Sage Green), Section B (Slate Blue), Section C (Amber).
- Text: Bold English with smaller Korean subtitles (e.g., **Data Sources**<br>데이터 소스).
