A clean, content-rich technical diagram of the 'Edge-RAG-Anything (ERA)' architecture. Style: Professional Academic Infographic (Original Style). Minimalist 2D/Flat style. White background. Color Palette: Slate Blue (Core), Sage Green (Domain), Amber (Logic).

Layout:
1. **Input**: 'User Query' -> **'Intent Analysis'** (Splits flow).

2. **Path A (Question - Detailed Internals)**:
   - 'Intent Analysis' -> **'Query Analysis'** (Tags).
   - -> **'Hybrid Retrieval'** (Box containing: **'Dense'**, **'Sparse'**, **'PathRAG'**).
   - -> **'Dynamic Weighting'**.
   - -> **'Resource Check'** (Diamond).
   - -> Branch High RAM: **'Cross-Encoder'**.
   - -> Branch Low RAM: **'LLM Judge'**.
   - -> **'Qwen3-4B'**.

3. **Path B (Task Request - Control Flow)**:
   - 'Intent Analysis' -> **'LLM (Action Plan)'**.
   - -> **'User Feedback/Permission'** (Explicit Check).
   - -> **'Device Controller'** -> **Smart Farm Devices** (Fan, Heater, Pump).
   - -> **'Execution Result'**.
   - -> **'Qwen3-4B'** (Merge point).

4. **Unified Output**:
   - Both Path A (Retrieval Context) and Path B (Execution Result) feed into **'Qwen3-4B'**.
   - **'Qwen3-4B'** generates the **'Final Answer'**.

5. **Visuals**: Show ALL components clearly. Ensure arrows show the convergence at Qwen3-4B.
